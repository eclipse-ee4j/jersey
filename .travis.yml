language: java

jdk:
  - oraclejdk8

cache:
  directories:
    - .autoconf
    - $HOME/.m2

install: true

script:
  - mvn -e -U -B -DfailIfNoTests=false -Dtest=!org.glassfish.jersey.tests.e2e.json.JaxbTest* clean install
