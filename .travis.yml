language: java

jdk:
  - oraclejdk8

cache:
  directories:
    - .autoconf
    - $HOME/.m2

install: true

#before_install:
#    - rm -rf ~/.linuxbrew
#    - sh -c "$(curl -fsSL https://raw.githubusercontent.com/Linuxbrew/install/master/install.sh)"
#    - export PATH="$HOME/.linuxbrew/bin:$PATH"
#    - export MANPATH="$HOME/.linuxbrew/share/man:$MANPATH"
#    - export INFOPATH="$HOME/.linuxbrew/share/info:$INFOPATH"
#    - brew install jdk
#    - export JAVA_HOME=/home/travis/.linuxbrew/Cellar/jdk/1.8.0-151

script:
  - mvn -e -U -B -Dtest=!org.glassfish.jersey.tests.e2e.json.JaxbTest* clean install
